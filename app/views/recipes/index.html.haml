#recipe-list
  .subheader
    %h3
      = @subheader
  - @recipes.each do |recipe|
    .recipe-name
      = link_to recipe.name, recipe_path(recipe)
#comment-list
  .subheader
    %h3
      Comments
  .form-container
    = link_to 'Add Comment', '#', :class => 'form-toggle-link'
    = form_for Comment.new, :html => {:class => 'hidden-form comment-form'} do |f|
      .label
        Name
      = f.text_field :user_name
      .label
        Comment
      = f.text_area :content
      = f.submit :class => 'submit button'
      = f.submit 'Cancel', :class => 'cancel button'
  .comments
    - Comment.limit(50).each do |comment|
      .comment
        .user-name= "#{comment.user_name} said:"
        .content= comment.content
